namespace gear.assets;

table TileSet {
    texture: uint64;
    textureWidth : short;
    textureHeight : short;
    tileWidth: short;
    tileHeight: short;
    tileCount: short;
    columns: short;
}

root_type TileSet;