include "common.fbs";

namespace gear.assets;

table TileSet {
    texture: Ref;
    textureWidth : short;
    textureHeight : short;
    tileWidth: short;
    tileHeight: short;
    tileCount: short;
    columns: short;
}

root_type TileSet;