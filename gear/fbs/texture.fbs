namespace gear.assets;

enum PixelFormat : ubyte{
    R8,
    RGB8,
    RGBA8,
}

table Texture {
    width: short;
    height: short;
    format: PixelFormat;
    data: [ubyte];
}

table TextureRef {
    name: uint64 (hash: "fnv1_64");
    ptr: Texture;
}
root_type Texture;