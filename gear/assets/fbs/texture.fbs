namespace gear.assets;

enum PixelFormat : ubyte{
    Invalid,
    R8,
    RGB8,
    RGBA8,
    BC7
}

table Texture {
    width: short;
    height: short;
    format: PixelFormat;
    data: [ubyte];
}
root_type Texture;